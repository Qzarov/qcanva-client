# Canvas Server — План разработки

## 1. Ноды
- [x] Отображение text, link, group нод
- [x] Markdown рендеринг внутри нод
- [x] Перетаскивание блоков (drag & drop)
- [x] Ресайз нод
- [x] Редактирование текста нод (двойной клик)
- [x] Создание новых нод (двойной клик по пустому месту)
- [x] Удаление нод (Delete/Backspace)
- [x] Контекстное меню (смена цвета, удаление, дублирование)
- [x] Callouts в markdown (`> [!warning]`, `> [!note]`)
- [ ] File-ноды — drag & drop картинок, отображение image/PDF
- [ ] Link-ноды — iframe-превью сайта

## 2. Связи (edges)
- [x] Стрелки (bezier-кривые) между нодами
- [x] Создание связей — drag от коннектора ноды к другой
- [x] Удаление связей (выделение + кнопка/Delete)
- [x] Редактирование лейблов на стрелках (двойной клик)
- [x] Лейблы с фоном на стрелках
- [x] Стили стрелок — solid/dashed/dotted, 6 цветов
- [ ] Типы наконечников стрелок — стрелка на конце, стрелка на начале, стрелки с обеих сторон, без стрелок
- [ ] Проведение стрелки из середины другой стрелки (ответвление)

## 3. Взаимодействие
- [x] Pan & zoom (мышь, колёсико, touch/pinch)
- [x] Мультиселект (Shift/Ctrl + клик, рамкой)
- [x] Undo/redo (Ctrl+Z / Ctrl+Shift+Z)
- [x] Copy/paste нод (Ctrl+C / Ctrl+V)
- [x] Snap to grid (24px)
- [x] Горячие клавиши (Ctrl+A, Escape, Delete)

## 4. Навигация
- [x] Minimap (мини-карта в углу)

## 5. Сохранение
- [x] Экспорт в `.canvas` формат
- [x] Импорт `.canvas` файлов
- [x] Создание нового пустого canvas

## 6. Бэкенд
- [x] Хранение canvas-файлов в БД (SQLite + TypeORM)
- [x] REST API для CRUD операций с canvas
- [x] Раздача canvas по уникальной ссылке

## 7. Роутинг
- [x] Уникальные URL `/canvas/:id` для каждого canvas
- [x] Read-only режим для просмотра по ссылке (public canvas)
- [x] Режим редактирования для пользователей с правами

## 8. Авторизация
- [x] Регистрация и вход (email/пароль)
- [x] JWT-токены
- [x] Страницы логина и регистрации
- [ ] OAuth авторизация через Google
- [ ] Добавить логин на страницу канваса, если нет доступа

## 9. Управление пользователями и правами
- [x] Владелец canvas может приглашать других пользователей
- [x] Роли доступа к canvas: чтение / редактирование
- [x] Управление списком пользователей и их правами
- [x] Возможность отозвать доступ
- [x] Интеграция фронтенда с API
- [x] Dashboard со списком canvas
- [x] Шеринг UI (share panel)
- [x] Автосохранение при редактировании (debounce 1s)

---

## 10. Realtime collaboration
- [x] WebSocket подключение (Socket.io / WS)
- [x] Синхронизация изменений между пользователями в реальном времени
- [x] Курсоры других пользователей на canvas
- [x] Индикатор «кто сейчас онлайн» на canvas
- [ ] Разрешение конфликтов при одновременном редактировании

## 10.1. Migration path к полноценной collaboration model

Технический контракт и целевой протокол описаны в [COLLABORATION_PROTOCOL.md](/home/qzaro/nocode/codex/canvas-server-front/COLLABORATION_PROTOCOL.md:1).

### Этап 1. Зафиксировать сервер как source of truth
- [x] Добавить `revision` в сущность canvas и возвращать его в `GET /api/canvas/:id`
- [x] Перевести WebSocket `canvas-op` на payload вида `{ op, baseRevision, clientOpId }`
- [x] На сервере проверять `baseRevision` перед применением операции
- [x] При успешном применении операции увеличивать `revision` и broadcast'ить уже серверную версию события: `{ op, revision, clientOpId, userId }`
- [x] Оставить `canvas-update` только как fallback/checkpoint, а не как основную модель истины
- [x] В UI показывать forced resync при `revision mismatch`

### Этап 2. Добавить протокол resync и recovery
- [x] Добавить событие/endpoint `canvas-resync`, которое возвращает полный snapshot + актуальный `revision`
- [x] На клиенте при пропуске ревизии или reject операции делать reload состояния через resync, а не молча продолжать работу
- [x] Ввести явные причины reject'а операции: `revision_mismatch`, `forbidden`, `invalid_op`, `target_missing`
- [ ] Добавить retry policy только для безопасных случаев, без бесконечного повторного применения
- [x] Логировать частоту resync/reject, чтобы видеть реальные проблемные сценарии

### Этап 3. Ввести op log и базовую историю
- [x] Создать таблицу `canvas_operations` с `canvasId`, `revision`, `clientOpId`, `userId`, `type`, `payload`, `createdAt`
- [x] Сохранять каждую принятую сервером операцию в op log
- [x] Использовать op log для дебага, replay и будущего diff/history
- [x] Добавить snapshot/checkpoint стратегию, чтобы не восстанавливать canvas только из длинной цепочки операций
- [x] Подготовить API для просмотра истории изменений canvas

### Этап 4. Формализовать conflict policy
- [x] Зафиксировать правила merge/reject по типам операций
- [x] Разрешить параллельное редактирование разных нод/рёбер без полного resync
- [ ] Для одной и той же ноды на первом этапе использовать простое правило `last-writer-wins` на уровне поля
- [x] Для конфликтов `delete vs update` и `delete vs move` возвращать reject/resync вместо неявного восстановления удалённого объекта
- [ ] Добавить optimistic UI с подтверждением операции сервером

### Этап 5. Улучшить collaborative text editing
- [ ] Выделить редактирование текста ноды в отдельный поток операций, а не смешивать его с геометрией
- [ ] Решить отдельно модель для text nodes: сначала coarse-grained patching, позже OT/CRDT при необходимости
- [ ] Не тащить CRDT сразу на весь canvas-документ

### Переходный режим
- [ ] На время миграции поддерживать старый `canvas-update` только для совместимости со старым клиентом
- [ ] Новому клиенту использовать `revision`-aware `canvas-op` как основной канал изменений
- [ ] После стабилизации telemetry и resync flow удалить legacy full-state sync из realtime-контура

### Критерий завершения миграции
- [ ] Сервер определяет единый порядок операций
- [ ] Клиент не может молча перетереть чужие изменения полным snapshot'ом
- [ ] При рассинхроне есть понятный и воспроизводимый путь recovery
- [ ] История операций позволяет строить versioning, diff и audit без переписывания архитектуры

## 11. Расширенные типы нод
- [ ] File-ноды — drag & drop картинок на canvas
- [ ] Загрузка файлов на сервер (image, PDF)
- [ ] Отображение картинок внутри нод
- [ ] Отображение PDF внутри нод
- [ ] Link-ноды — iframe-превью сайтов
- [ ] Превью метаданных ссылки (title, description, favicon)

## 12. Поиск
- [ ] Поиск по canvas (Ctrl+F) — находит ноды по тексту
- [ ] Подсветка найденных нод
- [ ] Навигация между результатами (Enter / Shift+Enter)
- [ ] Поиск по всем canvas на dashboard

## 13. Версионирование
- [ ] Автоматическое сохранение версий canvas
- [ ] Просмотр истории изменений
- [ ] Откат к предыдущей версии
- [ ] Diff между версиями (визуальное сравнение)

## 14. Комментарии
- [ ] Добавление комментария к ноде
- [ ] Тред комментариев (ответы)
- [ ] Индикатор комментариев на ноде
- [ ] Панель комментариев (sidebar)
- [ ] Уведомления о новых комментариях

## 15. Организация canvas
- [ ] Теги для canvas
- [ ] Фильтрация и поиск по тегам на dashboard
- [ ] Папки / группировка canvas
- [ ] Сортировка (по дате, имени, последнему изменению)
- [ ] Избранное (pin canvas)

## 16. Темплейты
- [ ] Встроенные шаблоны (roadmap, mindmap, kanban, flowchart)
- [ ] Создание canvas из шаблона
- [ ] Сохранение своего canvas как шаблона
- [ ] Галерея шаблонов на dashboard

## 17. Presentation mode
- [ ] Режим презентации — показ canvas как слайд-шоу
- [ ] Навигация по нодам в заданном порядке
- [ ] Плавные переходы (zoom + pan анимация)
- [ ] Полноэкранный режим
- [ ] Настройка порядка слайдов
