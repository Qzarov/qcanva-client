# Copy this file to android/keystore.properties. The real file is ignored by Git.
# Keep the upload key in a password manager or a dedicated secrets vault, never in this repository.
storeFile=/absolute/path/to/qcanva-upload.jks
storePassword=change-me
keyAlias=qcanva-upload
keyPassword=change-me
